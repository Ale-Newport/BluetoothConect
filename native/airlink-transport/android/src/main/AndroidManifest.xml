<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools">

    <!--
      Permissions the transport layer needs. Declared in the library so an app
      consuming it cannot forget one, and merged into the app manifest.

      The neverForLocation flag on BLUETOOTH_SCAN and NEARBY_WIFI_DEVICES is a
      promise to the system, and to the user, that AirLink does not use these
      radios to work out where anyone is. It is also what lets us avoid asking
      for ACCESS_FINE_LOCATION at all on Android 12 and later.
    -->

    <!-- Android 11 and earlier -->
    <uses-permission android:name="android.permission.BLUETOOTH" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" android:maxSdkVersion="30" />
    <!-- Required on API 23-30 for BLE scanning, and only for that. -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" android:maxSdkVersion="30" />

    <!-- Android 12+ -->
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN"
                     android:usesPermissionFlags="neverForLocation"
                     tools:targetApi="s" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADVERTISE" tools:targetApi="s" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" tools:targetApi="s" />

    <!-- Android 13+: Wi-Fi Direct, Wi-Fi Aware and local-only hotspot -->
    <uses-permission android:name="android.permission.NEARBY_WIFI_DEVICES"
                     android:usesPermissionFlags="neverForLocation"
                     tools:targetApi="tiramisu" />

    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <!--
      A foreground service is the only supported way to keep a Bluetooth session
      alive while the app is in the background on Android 14+. It costs a
      visible notification, which is why it is started only while a session is
      actually live.
    -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!--
      The microphone, for voice messages only. AirLink records when the user
      holds the record button in a chat and at no other time; there is no
      background listening, no transcription, and nowhere for a recording to go
      except to the phone the user is talking to.
    -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-feature android:name="android.hardware.microphone" android:required="false" />

    <uses-feature android:name="android.hardware.bluetooth_le" android:required="true" />
    <uses-feature android:name="android.hardware.wifi.direct" android:required="false" />
    <uses-feature android:name="android.hardware.wifi.aware" android:required="false" />
</manifest>
